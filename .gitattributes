# LF везде. Правится этот репозиторий с Windows, а исполняется в контейнере
# Home Assistant на Linux; CRLF в JSON переводов и в YAML воркфлоу проходит
# незаметно, а в shell-скриптах и в .env ломает всё молча.
* text=auto eol=lf
